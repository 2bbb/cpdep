{
  "name": "cpdep",
  "version": "0.0.2",
  "description": "analyse & copy dependencies on C++ library",
  "bin": {
    "cpdep": "index.js"
  },
  "preferGlobal": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["C++", "dependency", "dependencies", "analyse", "copy"],
  "author": "ISHII 2bit",
  "license": "MIT",
  "dependencies": {
    "commander": "^2.9.0",
    "fs-extra": "^1.0.0"
  }
}
